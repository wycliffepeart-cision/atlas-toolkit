import { makeIcon } from '../../utils/makeIcon';
import Projects from '../../../assets/icons/projects.svg';

export const AtlasIconProjectsName = 'atlas-icon-projects';

makeIcon(AtlasIconProjectsName, Projects);
